galaxy_info:
    author: elouin
    description: Installing ansible via pip
    company: 

    license: Apache

    min_ansible_version: 2.2

    platforms:
        - name: CentOS
          versions:
          - 6
          - 7

    galaxy_tags:
        - ansible
        - deployment

    dependencies: []
